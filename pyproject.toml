[project]
name = "p3-dacon"
version = "0.1.0"
description = "실전융합프로젝트3 기말 프로젝트"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "catboost>=1.2.10",
    "numpy>=2.4.6",
    "pandas>=3.0.3",
    "pyarrow>=24.0.0",
    "scikit-learn>=1.9.0",
]
